import React from "react";
import './header.scss';

function Header () {
    return (
      <div className="header">
        <h1 className="header__main">The Great HTML Escape</h1>
        <h2 className="header__secondary">We should always follow best practices... our should we?</h2>
      </div>
    );
  };

export default Header;
